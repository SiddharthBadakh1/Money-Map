<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Goal</title>
    <link rel="stylesheet" href="/css/styles1.css">
</head>
<body>
    <div class="container">
        <h1>Update Goal: <%= goal.name %></h1>
        <form action="/update-goal/<%= goal.name %>" method="POST">
            <label for="saved">Amount Saved (₹):</label>
            <input type="number" id="saved" name="saved" value="<%= goal.saved %>" required>
            <button type="submit">Update Goal</button>
        </form>
    </div>
</body>
</html>